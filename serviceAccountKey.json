var admin = require("firebase-adminsdk-fbsvc@telegrambot-fa481.iam.gserviceaccount.com");

var serviceAccount = require("path/to/serviceAccountKey.json");

admin.initializeApp({
  credential: admin.credential.cert(serviceAccount)
});
